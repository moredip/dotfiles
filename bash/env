export EDITOR="vim"
export PATH=$PATH:~/bin:~/local/bin:/usr/local/share/npm/bin
export HISTFILESIZE=30000
set -o vi

# hit Ctrl-Space to do history substitution before actually running the current command
bind "\C-Space":magic-space 

export GREP_OPTIONS="--color=auto"
